spring:
  datasource:
    url: 'DB 접속 주소'
    driver-class-name: org.mariadb.jdbc.Driver
    username: '아이디'
    password: '비밀번호'

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
